# FlowDesk — Project Instructions

**Before doing anything else in this project, read `HANDOFF.md` in the repo
root.** It carries the goal, current state, in-progress work, active file,
recent changes, failed attempts, and next steps across sessions.

As work progresses, keep `HANDOFF.md` up to date: update the relevant
section(s) when you make a change, hit a dead end, or wrap up a session — so
the next session can resume without re-discovering context.
